package models

type {{.CapsResourceNameSingular}} struct {
    Id int64 `json:"id,omitempty"`
{{range $k, $v := .Fields}}
    {{if eq $k "Id"}}
    {{else}}
    {{$k}} {{$v}} `json:"{{toLowerCase $k}},omitempty"`
    {{end}}
{{end}}
}